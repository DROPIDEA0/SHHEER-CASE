# دليل النشر على السيرفر - SHHEER CASE

## معلومات السيرفر

**IP:** 72.62.7.159  
**المستخدم:** root  
**كلمة المرور:** Downy144168@#5519971930

---

## 📋 خطوات النشر

### 1. الاتصال بالسيرفر

```bash
ssh root@72.62.7.159
```

### 2. الانتقال لمجلد المشروع

```bash
cd /var/www/shheer-case
# أو المسار الفعلي للمشروع على السيرفر
```

### 3. سحب آخر التحديثات من GitHub

```bash
git pull origin main
```

**ملاحظة:** بما أن المشروع مربوط بـ webhook كـ auto-deploy، فإن التحديثات ستُطبق تلقائياً عند push إلى GitHub. إذا لم يحدث ذلك، قم بتنفيذ الأمر أعلاه يدوياً.

### 4. تطبيق تحديثات قاعدة البيانات

```bash
# الاتصال بقاعدة البيانات وتطبيق الملف
mysql -u shheer-case-user -p shheer-case-name < database/migration_update.sql
```

**بيانات قاعدة البيانات:**
- اسم المستخدم: `shheer-case-user` (تحقق من ملف .env)
- اسم قاعدة البيانات: `shheer-case-name`
- كلمة المرور: (موجودة في ملف .env على السيرفر)

### 5. إنشاء مجلدات الرفع

```bash
# إنشاء مجلدات الرفع إذا لم تكن موجودة
mkdir -p dist/public/uploads/{logos,favicons,videos,evidence,documents}

# تعيين الصلاحيات المناسبة
chmod -R 755 dist/public/uploads
chown -R www-data:www-data dist/public/uploads
```

**ملاحظة:** قد يختلف المستخدم حسب إعدادات السيرفر (www-data أو nginx أو apache)

### 6. تثبيت التبعيات (إذا لزم الأمر)

```bash
pnpm install
```

### 7. بناء المشروع

```bash
pnpm build
```

### 8. إعادة تشغيل التطبيق

إذا كان المشروع يعمل بـ PM2:
```bash
pm2 restart all
# أو
pm2 restart shheer-case
```

إذا كان يعمل بـ systemd:
```bash
systemctl restart shheer-case
```

إذا كان يعمل بـ Docker:
```bash
docker-compose down
docker-compose up -d
```

---

## 🔍 التحقق من النشر

### 1. التحقق من حالة التطبيق

```bash
# إذا كان يعمل بـ PM2
pm2 status

# إذا كان يعمل بـ systemd
systemctl status shheer-case

# إذا كان يعمل بـ Docker
docker-compose ps
```

### 2. فحص السجلات (Logs)

```bash
# PM2
pm2 logs shheer-case

# systemd
journalctl -u shheer-case -f

# Docker
docker-compose logs -f
```

### 3. اختبار الموقع

افتح المتصفح وتوجه إلى:
- **الموقع الرئيسي:** https://shheer-case.com (أو النطاق الفعلي)
- **لوحة التحكم:** https://shheer-case.com/admin/login

**بيانات الدخول:**
- اسم المستخدم: `admin`
- كلمة المرور: `admin123`

---

## ✅ قائمة التحقق بعد النشر

### اختبار رفع الملفات

- [ ] **1. اختبار رفع اللوجو**
  - الدخول إلى `/admin/settings` > تبويب Branding
  - رفع شعار جديد
  - التحقق من ظهوره في لوحة التحكم

- [ ] **2. اختبار رفع الفيديو**
  - الدخول إلى `/admin/videos`
  - رفع فيديو جديد
  - التحقق من عدم وجود تحميل لا نهائي
  - التحقق من ظهور الفيديو على الموقع

- [ ] **3. اختبار المستندات الرسمية**
  - الدخول إلى `/admin/official-documents`
  - رفع مستند PDF أو Word
  - إضافة عنوان ووصف وتصنيف
  - حفظ المستند
  - التحقق من ظهور قسم "المستندات الرسمية" على الموقع
  - اختبار معاينة وتحميل المستند

- [ ] **4. اختبار زر واتساب**
  - الدخول إلى `/admin/whatsapp`
  - تفعيل الزر
  - إدخال رقم واتساب صحيح (مثال: +966500000000)
  - كتابة رسالة افتراضية
  - اختيار موضع الزر
  - حفظ الإعدادات
  - التحقق من ظهور الزر على الموقع
  - اختبار النقر على الزر وفتح محادثة واتساب

---

## 🐛 حل المشاكل الشائعة

### مشكلة: الملفات لا تُرفع

**الحل:**
```bash
# التحقق من وجود مجلدات الرفع
ls -la dist/public/uploads/

# إنشاء المجلدات إذا لم تكن موجودة
mkdir -p dist/public/uploads/{logos,favicons,videos,evidence,documents}

# تعيين الصلاحيات
chmod -R 755 dist/public/uploads
chown -R www-data:www-data dist/public/uploads
```

### مشكلة: قاعدة البيانات لا تحتوي على الجداول الجديدة

**الحل:**
```bash
# تطبيق ملف SQL يدوياً
mysql -u shheer-case-user -p shheer-case-name < database/migration_update.sql

# التحقق من وجود الجداول
mysql -u shheer-case-user -p shheer-case-name -e "SHOW TABLES;"
```

### مشكلة: التطبيق لا يعمل بعد النشر

**الحل:**
```bash
# التحقق من السجلات
pm2 logs shheer-case --lines 50

# إعادة بناء المشروع
pnpm build

# إعادة تشغيل التطبيق
pm2 restart all
```

### مشكلة: webhook لا يعمل

**الحل:**
1. التحقق من إعدادات webhook في GitHub
2. التحقق من سجلات webhook في GitHub > Settings > Webhooks
3. التحقق من سكريبت auto-deploy على السيرفر
4. تطبيق التحديثات يدوياً باستخدام `git pull`

---

## 📞 الدعم الفني

في حالة وجود أي مشاكل، يرجى التواصل مع:

**شركة دروب أيديا (DROPIDEA)**
- الموقع: https://dropidea.com
- البريد الإلكتروني: support@dropidea.com

---

## 📝 ملاحظات مهمة

1. **النسخ الاحتياطي:** تأكد من عمل نسخة احتياطية من قاعدة البيانات قبل تطبيق التحديثات:
   ```bash
   mysqldump -u shheer-case-user -p shheer-case-name > backup_$(date +%Y%m%d_%H%M%S).sql
   ```

2. **متغيرات البيئة:** تأكد من وجود ملف `.env` على السيرفر يحتوي على:
   - `DATABASE_URL` أو بيانات الاتصال بقاعدة البيانات
   - `PORT` (عادة 3000 أو 5000)
   - أي متغيرات أخرى مطلوبة

3. **الصلاحيات:** تأكد من أن مجلدات الرفع لها الصلاحيات المناسبة (755 أو 775)

4. **الأمان:** تأكد من تغيير كلمة مرور الأدمن الافتراضية بعد النشر

---

**تاريخ التحديث:** 23 ديسمبر 2025  
**الإصدار:** 2.3.0
