# SHHEER Case - قائمة المهام والتحسينات

## التعديلات المكتملة ✅

### 1. إصلاح لوحة التحكم (Admin Panel)
- [x] إصلاح مشكلة التحميل المستمر في لوحة التحكم
- [x] إضافة وضع التطوير (Dev Mode) للوصول بدون OAuth

### 2. ميزة رفع الفيديوهات
- [x] إضافة إمكانية رفع الفيديوهات مباشرة على السيرفر
- [x] دعم صيغ: MP4, WebM, OGG, MOV (حد أقصى 500MB)
- [x] إضافة رفع الصور المصغرة (Thumbnails)
- [x] تبويبان للاختيار: URL أو Upload

### 3. ميزة تعديل ألوان Timeline
- [x] إضافة أعمدة الألوان المخصصة في قاعدة البيانات:
  - `customColor` - لون الشارة
  - `customBgColor` - لون خلفية البوكس
  - `customTextColor` - لون النص
- [x] إضافة واجهة اختيار الألوان في لوحة التحكم
- [x] معاينة مباشرة للألوان قبل الحفظ

### 4. إصلاح Preloader
- [x] إرجاع Preloader للشكل الأصلي (اللوجو مع خلفية فاتحة)

---

## المهام المطلوبة 📋

### 1. تشغيل زر Logout للوحة التحكم
- [ ] تفعيل زر تسجيل الخروج في لوحة التحكم
- [ ] إضافة تأكيد قبل تسجيل الخروج
- [ ] إعادة التوجيه لصفحة تسجيل الدخول بعد الخروج

### 2. نظام تأمين الموقع بكلمة مرور (Site Protection)
- [ ] إضافة خيار في لوحة التحكم لتفعيل/تعطيل حماية الموقع
- [ ] عند التفعيل: لا يمكن لأحد تصفح الموقع إلا بإدخال Username و Password
- [ ] إنشاء جدول في قاعدة البيانات للمستخدمين المصرح لهم:
  ```sql
  CREATE TABLE site_access_users (
    id INT AUTO_INCREMENT PRIMARY KEY,
    username VARCHAR(100) NOT NULL UNIQUE,
    password VARCHAR(255) NOT NULL, -- مشفرة
    name VARCHAR(200),
    isActive BOOLEAN DEFAULT true,
    createdAt TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updatedAt TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
  );
  ```
- [ ] واجهة في لوحة التحكم لإدارة المستخدمين:
  - إضافة مستخدم جديد
  - تعديل كلمة المرور
  - حذف مستخدم
  - تفعيل/تعطيل مستخدم
- [ ] صفحة تسجيل دخول للموقع المحمي

### 3. نظام تسجيل دخول مخصص للوحة التحكم (Admin Login)
- [ ] صفحة تسجيل دخول مخصصة للوحة التحكم
- [ ] عرض الشعار أعلى صفحة الدخول
- [ ] الدخول عبر Username و Password
- [ ] إنشاء جدول للمشرفين:
  ```sql
  CREATE TABLE admin_users (
    id INT AUTO_INCREMENT PRIMARY KEY,
    username VARCHAR(100) NOT NULL UNIQUE,
    password VARCHAR(255) NOT NULL, -- مشفرة
    name VARCHAR(200),
    email VARCHAR(320),
    role ENUM('super_admin', 'admin', 'editor', 'viewer') DEFAULT 'editor',
    permissions JSON, -- صلاحيات مخصصة
    isActive BOOLEAN DEFAULT true,
    lastLogin TIMESTAMP,
    createdAt TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updatedAt TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
  );
  ```
- [ ] نظام الأدوار والصلاحيات (Roles & Permissions):
  - **Super Admin**: جميع الصلاحيات + إدارة المشرفين
  - **Admin**: جميع الصلاحيات ما عدا إدارة المشرفين
  - **Editor**: تعديل المحتوى فقط
  - **Viewer**: عرض فقط بدون تعديل
- [ ] واجهة إدارة الموظفين/المشرفين في لوحة التحكم

### 4. إضافة سطر الدعم التقني (Footer Credit)
- [ ] إضافة سطر أسفل لوحة التحكم:
  > الدعم التقني والتصميم من قبل شركة [دروب أيديا](https://dropidea.com)
- [ ] إضافة نفس السطر أسفل الموقع الرئيسي
- [ ] رابط "دروب أيديا" يفتح في تبويب جديد ويوجه إلى https://dropidea.com

---

## ملاحظات تقنية 📝

### الملفات المعدلة حتى الآن:
- `server/_core/context.ts` - إضافة وضع التطوير
- `client/src/pages/admin/VideosAdmin.tsx` - ميزة رفع الفيديوهات
- `client/src/pages/admin/TimelineAdmin.tsx` - ميزة الألوان المخصصة
- `client/src/components/Timeline.tsx` - دعم الألوان المخصصة
- `drizzle/schema.ts` - إضافة أعمدة الألوان
- `server/routers.ts` - تحديث API endpoints
- `client/index.html` - إصلاح Preloader

### قاعدة البيانات:
- ملف التصدير المحدث: `shheer_case_database_updated.sql`
- يجب تنفيذه على السيرفر لإضافة الأعمدة الجديدة

---

## تاريخ التحديث
- **2024-12-21**: إنشاء الملف وتوثيق المهام
