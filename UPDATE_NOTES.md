# تحديثات المشروع - 23 ديسمبر 2025

## ملخص التحديثات

تم إجراء تحديثات شاملة على مشروع SHHEER CASE لإصلاح مشاكل رفع الملفات وإضافة ميزات جديدة.

---

## 🔧 الإصلاحات

### 1. إصلاح مشكلة رفع الملفات
**المشكلة:** فشل رفع الملفات (اللوجو، الفيديو، المستندات) بسبب الاعتماد على Manus Storage API غير المتوفر في بيئة الإنتاج.

**الحل:**
- إنشاء نظام رفع ملفات محلي جديد (`server/fileUpload.ts`)
- حفظ الملفات مباشرة على السيرفر في مجلد `dist/public/uploads/`
- تحديث جميع endpoints لاستخدام النظام الجديد:
  - `uploadAdminLogo` - رفع شعار لوحة التحكم
  - `uploadFavicon` - رفع أيقونة الموقع
  - `uploadVideo` - رفع الفيديوهات
  - `uploadFile` - رفع ملفات الأدلة
  - `uploadDocument` - رفع المستندات الرسمية (جديد)

**الملفات المعدلة:**
- `server/fileUpload.ts` (جديد)
- `server/routers.ts`
- `server/db.ts`

---

## ✨ الميزات الجديدة

### 2. قسم المستندات الرسمية (Official Documents)

**الوصف:** قسم جديد لرفع وعرض الملفات الكبيرة مثل البروفايلات، قرارات المحكمة، والدراسات القانونية.

**المميزات:**
- رفع ملفات حتى 100 ميجابايت
- دعم صيغ متعددة: PDF, Word, Excel, PowerPoint, ZIP
- تصنيف المستندات حسب الفئات
- معاينة وتحميل المستندات
- عرض حجم الملف ونوعه
- ترتيب قابل للتخصيص
- تفعيل/تعطيل المستندات

**الملفات الجديدة:**
- `drizzle/schema.ts` - إضافة جدول `official_documents`
- `server/db.ts` - دوال CRUD للمستندات
- `server/routers.ts` - API endpoints
- `client/src/pages/admin/OfficialDocumentsAdmin.tsx` - صفحة الإدارة
- `client/src/components/OfficialDocuments.tsx` - عرض المستندات على الموقع
- `database/add_official_documents.sql` - SQL للجدول الجديد

**الموقع على الموقع:** بعد قسم Video Documentation

---

### 3. زر واتساب الثابت (WhatsApp Floating Button)

**الوصف:** زر واتساب ثابت يظهر في زاوية الموقع مع إمكانية التحكم الكامل من لوحة الإدارة.

**المميزات:**
- تفعيل/تعطيل الزر من لوحة التحكم
- تخصيص رقم الواتساب
- رسالة افتراضية قابلة للتخصيص
- اختيار موضع الزر (4 خيارات):
  - أسفل اليمين
  - أسفل اليسار
  - أعلى اليمين
  - أعلى اليسار
- تأثيرات حركية جذابة (Pulse Animation)
- معاينة مباشرة للإعدادات

**الملفات الجديدة:**
- `drizzle/schema.ts` - إضافة جدول `whatsapp_settings`
- `server/db.ts` - دوال إدارة إعدادات واتساب
- `server/routers.ts` - API endpoints
- `client/src/pages/admin/WhatsAppSettingsAdmin.tsx` - صفحة الإعدادات
- `client/src/components/WhatsAppButton.tsx` - مكون الزر الثابت
- `database/add_whatsapp_settings.sql` - SQL للجدول الجديد

**الوصول من لوحة التحكم:** `/admin/whatsapp`

---

## 📁 هيكل الملفات الجديدة

### مجلدات الرفع
```
dist/public/uploads/
├── logos/          # شعارات لوحة التحكم
├── favicons/       # أيقونات الموقع
├── videos/         # الفيديوهات
├── evidence/       # ملفات الأدلة
└── documents/      # المستندات الرسمية
```

### قاعدة البيانات
```
database/
├── migration_update.sql          # ملف SQL شامل لجميع التحديثات
├── add_official_documents.sql    # جدول المستندات الرسمية
└── add_whatsapp_settings.sql     # جدول إعدادات واتساب
```

---

## 🗄️ تحديثات قاعدة البيانات

### جداول جديدة

#### 1. official_documents
| العمود | النوع | الوصف |
|--------|------|-------|
| id | int | المعرف الفريد |
| title | varchar(300) | عنوان المستند |
| description | text | وصف المستند |
| fileUrl | text | رابط الملف |
| fileName | varchar(300) | اسم الملف |
| fileType | varchar(50) | نوع الملف |
| fileSize | varchar(50) | حجم الملف |
| category | varchar(100) | التصنيف |
| displayOrder | int | ترتيب العرض |
| isActive | tinyint(1) | حالة التفعيل |
| createdAt | timestamp | تاريخ الإنشاء |
| updatedAt | timestamp | تاريخ التحديث |

#### 2. whatsapp_settings
| العمود | النوع | الوصف |
|--------|------|-------|
| id | int | المعرف الفريد |
| isEnabled | tinyint(1) | حالة التفعيل |
| phoneNumber | varchar(50) | رقم الواتساب |
| message | text | الرسالة الافتراضية |
| position | varchar(20) | موضع الزر |
| createdAt | timestamp | تاريخ الإنشاء |
| updatedAt | timestamp | تاريخ التحديث |

---

## 🚀 خطوات التطبيق على السيرفر

### 1. تطبيق تحديثات قاعدة البيانات

```bash
# الاتصال بالسيرفر
ssh root@72.62.7.159

# الانتقال لمجلد المشروع
cd /path/to/shheer-case

# تطبيق ملف SQL الشامل
mysql -u shheer-case-user -p shheer-case-name < database/migration_update.sql
```

### 2. إنشاء مجلدات الرفع

```bash
# إنشاء مجلدات الرفع
mkdir -p dist/public/uploads/{logos,favicons,videos,evidence,documents}

# تعيين الصلاحيات
chmod -R 755 dist/public/uploads
```

### 3. تحديث المشروع من GitHub

```bash
# سحب آخر التحديثات
git pull origin main

# تثبيت التبعيات
pnpm install

# بناء المشروع
pnpm build

# إعادة تشغيل السيرفر
pm2 restart all
```

---

## 🔍 الاختبار

### 1. اختبار رفع الملفات
- ✅ الدخول إلى `/admin/settings` > Branding
- ✅ رفع شعار جديد
- ✅ رفع أيقونة جديدة
- ✅ التحقق من ظهور الملفات

### 2. اختبار الفيديو
- ✅ الدخول إلى `/admin/videos`
- ✅ رفع فيديو جديد (حتى 500MB)
- ✅ التحقق من التحميل والعرض

### 3. اختبار المستندات الرسمية
- ✅ الدخول إلى `/admin/official-documents`
- ✅ رفع مستند جديد (حتى 100MB)
- ✅ إضافة تصنيف ووصف
- ✅ التحقق من ظهور القسم على الموقع

### 4. اختبار واتساب
- ✅ الدخول إلى `/admin/whatsapp`
- ✅ تفعيل الزر
- ✅ إدخال رقم واتساب
- ✅ تخصيص الرسالة والموضع
- ✅ التحقق من ظهور الزر على الموقع
- ✅ اختبار فتح محادثة واتساب

---

## 📋 قائمة التحقق (Checklist)

- [x] إصلاح مشكلة رفع اللوجو
- [x] إصلاح مشكلة رفع الفيديو
- [x] إضافة قسم المستندات الرسمية
- [x] إضافة صفحة إدارة المستندات
- [x] إضافة عرض المستندات على الموقع
- [x] إضافة جدول whatsapp_settings
- [x] إضافة صفحة إعدادات واتساب
- [x] إضافة زر واتساب الثابت
- [x] تحديث قائمة لوحة التحكم
- [x] تحديث ملفات التوجيه
- [x] إنشاء ملفات SQL للتحديثات
- [x] إنشاء وثائق التحديثات

---

## 🎯 الخطوات التالية

1. **رفع التحديثات إلى GitHub**
   ```bash
   git add .
   git commit -m "Fix: File upload system + Add Official Documents + WhatsApp button"
   git push origin main
   ```

2. **تطبيق التحديثات على السيرفر**
   - تشغيل ملف SQL
   - إنشاء مجلدات الرفع
   - إعادة تشغيل السيرفر

3. **الاختبار النهائي**
   - اختبار جميع الميزات الجديدة
   - التحقق من عمل رفع الملفات
   - اختبار زر واتساب

---

## 📞 الدعم الفني

**شركة دروب أيديا (DROPIDEA)**
- الموقع: https://dropidea.com

---

**تاريخ التحديث:** 23 ديسمبر 2025  
**الإصدار:** 2.3.0
