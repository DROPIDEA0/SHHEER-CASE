# Database Configuration
# Option 1: Use DATABASE_URL (recommended)
DATABASE_URL=mysql://username:password@localhost:3306/database_name

# Option 2: Use individual variables (will be combined if DATABASE_URL is not set)
DB_HOST=localhost
DB_PORT=3306
DB_USER=your_database_user
DB_PASSWORD=your_database_password
DB_NAME=your_database_name

# JWT Secret for Authentication
JWT_SECRET=your_jwt_secret_here_at_least_32_characters

# Application Settings
VITE_APP_TITLE=SHHEER Case - Bank Guarantee Dispute
VITE_APP_LOGO=/images/logo.png

# Node Environment
NODE_ENV=production
